0 9 * * * cd /srv/echo-universe-engine && uv run python agents/character_agent.py && git add . && git commit -m "daily character"
0 13 * * * cd /srv/echo-universe-engine && uv run python agents/lore_agent.py && git add . && git commit -m "daily lore"
0 18 * * * cd /srv/echo-universe-engine && uv run python agents/story_agent.py && git add . && git commit -m "daily story"
0 23 * * * cd /srv/echo-universe-engine && uv run python agents/consistency_agent.py && git add . && git commit -m "daily canon audit"
